{
  "name": "sushi-chat-shared",
  "version": "1.0.0",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "node": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "tsc -w",
    "build": "tsc",
    "lint": "eslint --ext \".js,.ts,.vue\" --ignore-path .gitignore ./src",
    "fix": "prettier --write \"**/*.{js,ts}\" --ignore-path .gitignore ./src",
    "fix:dry-run": "prettier --check \"**/*.{js,ts}\" --ignore-path .gitignore ./src",
    "test": "echo \"Error: run tests from root\" && exit 1"
  },
  "devDependencies": {
    "tsc-watch": "^4.2.9"
  }
}
